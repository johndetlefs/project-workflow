# EPIC-016 Dogfood Receipt

Date: 2026-08-24

## Scope

This receipt proves the current-task contract-loading, architectural subtraction, automatic
lifecycle-boundary behavior, and one disposable fresh-context handoff of the Coordinator Core
correction. It does not yet claim consolidated independent QA, publication, or rollout.

## Triggering Contradiction

The first candidate added a second execution graph to `COORDINATION.json`: units, dependency state,
packet references, receipt references, packet issuance, and return reconciliation. Existing Task
and Epic Delegate orchestration already owned those facts and gates. The behavioural evaluation
also exercised the Coordinator/Clarify/Planner/QA contract without calling the new packet or
reconcile commands. The green evidence therefore did not justify the duplicated CLI layer.

## Approved Correction

The owner approved the evidence-backed `Coordinator Core, not Coordination Platform` correction in
the current Codex task on 2026-08-24.

- `COORDINATION.json` contract version `2` records only logical phase, current Intent/source,
  material decisions, explicit context declaration, five named boundary decisions, one earliest
  checkpoint, and next action.
- The canonical plan and existing Delegate orchestration remain the sole owners of units,
  dependencies, packets, returns, evidence reconciliation, and worker lifecycle.
- Existing Task/Epic lifecycle transitions fail closed when their required current source-bound
  decision is missing, stale, or drifted. The owner does not need to remember a special prompt or
  command.
- The same physical context may continue after explicitly loading and declaring the current
  contract when there is no conflict or isolation need. This is a declaration of loaded authority,
  not a false proof of physical freshness. A fresh context must earn its transfer cost.
- Existing Delegate Task and Epic packets now include exact authority source/hash, invalid
  substitutes, and an explicit bounded return contract. `coordinate packet` and `coordinate
  reconcile` no longer exist.

## Current Dogfood Evidence

1. The owner-approved change is recorded as `approved-change` at
   `new-evidence-or-owner-reframe`, followed by a current `after-plan-or-decomposition` decision and
   source-bound child-start decisions.
2. Current-task preflight reports package `0.6.0`, asset `5`, coordination contract `2`, and
   `contract_state: current` after explicit loading was declared.
3. Doctor passes the compact schema and reports no active issue.
4. A lifecycle regression rejects `In Progress -> Testing` without the current
   `unit-return-or-dependency-join` decision, then permits the unchanged transition after that
   decision is recorded.
5. Focused coordination, subtraction, Clarify, and existing Task/Epic Delegate coverage passed
   before independent QA. The affected post-QA source-binding and evaluator set passes `21/21`;
   Task and Epic regressions now prove a boundary decision becomes stale after source revision.
6. `FRESH-CONTEXT-JOURNEY.md` records one current-contract disposable handoff that completed with
   exact authority/scope, two inside-envelope boundaries, zero owner questions, zero additional
   contexts and zero QA actions. Its host counters also show that fresh-context startup is not
   inherently inexpensive.

## Current Stop

One consolidated independent QA issued Changes Requested and is recorded in `INDEPENDENT-QA.md`.
All three findings are now resolved through affected source-binding tests, strict grader
counter-tests and the two Run 005 trials. Final dogfood also found and corrected the literal-Pass
completion contradiction: a retained Changes Requested verdict can now close through one exact
affected validation without rewriting history or commissioning QA again. The combined affected
set passes 39/39. No additional review loop is authorized; the existing package/release child is
the next delivery boundary. TASK-090 through TASK-093 are Complete; TASK-094 remains the sole
In Progress package, publication, and eligible-rollout child.
